<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>

    <!-- Package metadata -->
    <PackageId>KeenEye.Generators.Attributes</PackageId>
    <Description>Attributes for KeenEye ECS source generators</Description>
    <PackageTags>ecs;entity;component;system;source-generator;attributes</PackageTags>

    <!-- Must target netstandard2.0 for maximum analyzer compatibility -->
    <IsPackable>true</IsPackable>
  </PropertyGroup>

</Project>
